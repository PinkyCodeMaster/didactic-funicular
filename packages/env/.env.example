# Environment Configuration Example
# Copy this file to .env in your workspace root and fill in the actual values

# Application Environment
NODE_ENV=development
PORT=9000
LOG_LEVEL=info

# Database Configuration
# Replace with your actual database URL (PostgreSQL, MySQL, SQLite, etc.)
DATABASE_URL=postgresql://username:password@localhost:5432/database_name
# Optional: Database auth token (for services like Turso, PlanetScale, etc.)
DATABASE_AUTH_TOKEN=your_database_auth_token_here

# Better Auth Configuration
# Generate a secure random string for the secret (at least 32 characters)
BETTER_AUTH_SECRET=your_secure_random_secret_key_here_at_least_32_chars
# URL where your auth service is running
BETTER_AUTH_URL=http://localhost:9000

# Backend Configuration
# URL where your backend API is running
BACKEND_URL=http://localhost:9000

# Client-side Environment Variables
# These are exposed to the client-side code (NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_BACKEND_URL=http://localhost:9000

# Email Configuration
# Get your API key from Resend (https://resend.com)
RESEND_API_KEY=your_resend_api_key_here

# Additional Notes:
# - All URLs should include the protocol (http:// or https://)
# - For production, use HTTPS URLs and secure secrets
# - Never commit actual secrets to version control
# - The DATABASE_AUTH_TOKEN is optional and only needed for certain database providers